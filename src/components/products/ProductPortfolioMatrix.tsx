import React from 'react';
import type { ProductPortfolioItem } from '../../core/products/ProductPortfolioTypes';
import RiskBadge from '../shared/RiskBadge';

export default function ProductPortfolioMatrix({ items }: { items: ProductPortfolioItem[] }) {
  return <section className="rounded-2xl border border-[var(--border-color)] bg-[var(--bg-card)] p-5"><div className="mb-4"><span className="text-[10px] uppercase font-mono font-black text-[var(--text-secondary)]">Matriz estratégica</span><h2 className="text-lg font-black text-[var(--text-main)] mt-1">Prioridade do portfólio</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="text-[10px] uppercase font-mono text-[var(--text-secondary)] border-b border-[var(--border-color)]"><th className="py-3">#</th><th>Produto</th><th>Prontidão</th><th>Demanda</th><th>Tração</th><th>Score</th><th>Risco</th></tr></thead><tbody>{items.map((item) => <tr key={item.service.id} className="border-b border-[var(--border-color)]/60 last:border-0"><td className="py-3 font-black text-emerald-300">{item.strategicRank}</td><td><strong className="text-sm text-[var(--text-main)]">{item.service.shortName}</strong><p className="text-[11px] text-[var(--text-secondary)] max-w-xs">{item.recommendation}</p></td><td>{item.readinessScore}%</td><td>{item.demandScore}%</td><td>{item.tractionScore}%</td><td className="font-black">{item.portfolioScore}%</td><td><RiskBadge level={item.risk === 'saudável' ? 'healthy' : item.risk === 'atenção' ? 'attention' : 'overdue'} /></td></tr>)}</tbody></table></div></section>;
}
