import React from 'react';
import { AlertTriangle, Banknote, TrendingUp } from 'lucide-react';
import type { FinanceIntelligenceSummary } from '../../core/finance/FinanceIntelligenceTypes';
export default function FinanceExecutiveSnapshot({ summary, onOpenFinance }: { summary: FinanceIntelligenceSummary; onOpenFinance: () => void }) { return <section className="rounded-2xl border border-emerald-500/20 bg-emerald-500/5 p-5"><div className="flex items-start justify-between gap-4"><div><span className="text-[10px] uppercase font-mono tracking-[0.22em] text-emerald-300 font-black">Financeiro executivo</span><h2 className="text-lg font-black text-[var(--text-main)] mt-1">Receita e risco financeiro</h2></div><button type="button" onClick={onOpenFinance} className="rounded-xl border border-emerald-500/30 bg-emerald-500/10 px-4 py-2 text-xs font-black text-emerald-200">Abrir financeiro</button></div><div className="grid grid-cols-1 sm:grid-cols-3 gap-3 mt-4"><Metric icon={<Banknote className="w-4 h-4" />} label="MRR" value={money(summary.recurringRevenue)} helper="receita mensal"/><Metric icon={<TrendingUp className="w-4 h-4" />} label="Recebimento" value={`${summary.collectionRate}%`} helper={money(summary.receivedRevenue)}/><Metric icon={<AlertTriangle className="w-4 h-4" />} label="Vencido" value={money(summary.overdueRevenue)} helper={`${summary.criticalClients} clientes críticos`}/></div></section>; }
function Metric({ icon, label, value, helper }: { icon: React.ReactNode; label: string; value: React.ReactNode; helper: string }) { return <div className="rounded-xl border border-[var(--border-color)] bg-[var(--bg-card)] p-3"><div className="flex items-center gap-2 text-emerald-300">{icon}<span className="text-[9px] uppercase font-mono tracking-widest font-black">{label}</span></div><strong className="block text-xl font-black text-[var(--text-main)] mt-2">{value}</strong><span className="text-[10px] text-[var(--text-secondary)]">{helper}</span></div>; }
const money = (value: number) => new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 }).format(value || 0);
