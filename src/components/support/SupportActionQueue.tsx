import React from 'react';
import { CheckCircle2, Plus, Sparkles } from 'lucide-react';
import type { SupportPriorityAction } from '../../core/support/SupportIntelligenceTypes';

export default function SupportActionQueue({ items, existingTitles, onCreateTask, creatingId }: { items: SupportPriorityAction[]; existingTitles: Set<string>; onCreateTask: (item: SupportPriorityAction) => Promise<void>; creatingId: string | null }) {
  return <section className="rounded-2xl border border-[var(--border-color)] bg-[var(--bg-card)] p-5 space-y-4"><div><span className="text-[10px] uppercase font-mono tracking-[0.24em] text-[var(--text-secondary)] font-black">Orquestração</span><h2 className="text-lg font-black text-[var(--text-main)] mt-1 flex items-center gap-2"><Sparkles className="w-5 h-5 text-sky-300" />Ações recomendadas pela Beta</h2></div>{items.length === 0 ? <p className="text-sm text-[var(--text-secondary)]">Nenhuma ação crítica pendente.</p> : <div className="space-y-3">{items.map((item) => { const exists = existingTitles.has(item.taskTitle.toLocaleLowerCase('pt-BR')); return <article key={item.id} className="rounded-xl border border-[var(--border-color)] bg-[var(--bg-main)]/35 p-4"><div className="flex flex-col lg:flex-row lg:items-start lg:justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-sm font-black text-[var(--text-main)]">{item.title}</h3><span className="text-[10px] uppercase font-mono font-black text-amber-300">{item.priority}</span></div><p className="text-xs text-[var(--text-secondary)] mt-2 leading-relaxed">{item.description}</p></div><button type="button" disabled={exists || creatingId === item.id} onClick={() => void onCreateTask(item)} className="inline-flex items-center justify-center gap-2 rounded-lg border border-sky-500/25 bg-sky-500/10 px-3 py-2 text-xs font-black text-sky-200 disabled:opacity-50">{exists ? <CheckCircle2 className="w-4 h-4" /> : <Plus className="w-4 h-4" />}{exists ? 'Encaminhada' : creatingId === item.id ? 'Criando...' : 'Criar tarefa'}</button></div></article>; })}</div>}</section>;
}
