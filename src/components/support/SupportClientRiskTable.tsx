import React from 'react';
import type { SupportClientRiskItem } from '../../core/support/SupportIntelligenceTypes';
import RiskBadge from '../shared/RiskBadge';

export default function SupportClientRiskTable({ items }: { items: SupportClientRiskItem[] }) {
  return <section className="rounded-2xl border border-[var(--border-color)] bg-[var(--bg-card)] p-5 overflow-hidden"><div className="mb-4"><span className="text-[10px] uppercase font-mono tracking-[0.24em] text-[var(--text-secondary)] font-black">Risco por cliente</span><h2 className="text-lg font-black text-[var(--text-main)] mt-1">Carteira em atendimento</h2></div>{items.length === 0 ? <p className="text-sm text-[var(--text-secondary)]">Nenhum cliente com chamados ativos.</p> : <div className="overflow-x-auto"><table className="w-full text-left"><thead><tr className="text-[10px] uppercase font-mono text-[var(--text-secondary)]"><th className="py-3 pr-4">Cliente</th><th className="py-3 pr-4">Risco</th><th className="py-3 pr-4">Ativos</th><th className="py-3 pr-4">Críticos</th><th className="py-3 pr-4">SLA</th><th className="py-3">Motivo</th></tr></thead><tbody>{items.slice(0, 8).map((item) => <tr key={item.clientId} className="border-t border-[var(--border-color)] text-sm"><td className="py-3 pr-4 font-black text-[var(--text-main)]">{item.clientName}</td><td className="py-3 pr-4"><RiskBadge level={item.risk === 'critical' ? 'overdue' : item.risk === 'attention' ? 'attention' : 'healthy'} /></td><td className="py-3 pr-4 text-[var(--text-secondary)]">{item.openTickets}</td><td className="py-3 pr-4 text-[var(--text-secondary)]">{item.criticalTickets}</td><td className="py-3 pr-4 text-[var(--text-secondary)]">{item.breachedTickets}</td><td className="py-3 text-xs text-[var(--text-secondary)]">{item.mainReason}</td></tr>)}</tbody></table></div>}</section>;
}
